# 뽀모도로 타이머 겸 작업 기록·일기 웹앱 — Product Requirements Document (PRD) v5 (Final)

## 1. Project Overview

### 1.1 Project Definition
브라우저에서 파일 하나로 실행되는 완전 로컬형 뽀모도로 타이머 겸 작업 기록·일기 웹앱으로, 서버나 인터넷 연결 없이 뽀모도로 집중/휴식 사이클을 운영하고 세션별 작업 메모와 일별 완료 기록을 관리하며, 나아가 자유 분량의 일기를 작성·보관·열람하는 도구.

### 1.2 Background
사용자는 별도 설치나 서버 구축 없이, 브라우저에서 파일을 열기만 하면 즉시 사용할 수 있는 개인용 생산성 도구를 필요로 한다. 특히 장시간 사용 시에도 타이머 정확도가 보장되고, 새로고침이나 재접속에도 데이터가 유지되는 신뢰성이 핵심 요구사항이다.

기존 v4의 작업 메모는 Focus 세션 종료 시점에 남기는 한 줄 기록이므로, 하루를 돌아보며 남기는 긴 호흡의 회고를 담기에는 분량과 작성 시점이 모두 맞지 않는다. 세션 단위의 짧은 메모와는 별개로, 사용자가 원하는 시점에 원하는 분량으로 작성하고 이후 자유롭게 열람·수정할 수 있는 일기 기능이 필요하다.

또한 일기는 작성에 수 분에서 수십 분이 소요될 수 있으므로, 작성 도중 타이머 세션 종료 등 다른 이벤트가 발생하더라도 작성 중인 내용이 유실되지 않는 보호 장치가 함께 요구된다.

### 1.3 Target Environment
- 웹 브라우저 (로컬 HTML 파일을 직접 열어 실행, `file://` 프로토콜)
- 서버/백엔드 없음 (Serverless, 완전 클라이언트 사이드)
- 인터넷 연결 불필요 (완전 오프라인 동작)
- **지원 브라우저**: Chrome, Edge, Firefox 최신 데스크톱 버전에서 `file://` 프로토콜로 직접 실행하는 환경을 기준으로 하며, 해당 환경에서 `localStorage` 기반 데이터 지속성을 **최선 노력(best-effort) 기준으로 지원**한다. 브라우저 정책·저장 공간 등으로 실제 쓰기가 실패하는 경우의 처리는 EC-05에 정의한다. `file://` 환경에서 localStorage 접근 자체가 차단되는 브라우저(예: 구형 Safari 등)는 지원 범위에서 제외한다.

---

## 2. Goals & Success Definition

### 2.1 Project Goals
- 뽀모도로 기법 기반 집중/휴식 사이클을 정확하게 운영한다.
- 세션별 작업 내용을 기록하고 일별로 조회 가능하게 한다.
- 사용자가 원하는 시점에 자유 분량의 일기를 작성하고, 날짜순 목록으로 열람·수정할 수 있게 한다.
- 장시간·오프라인 환경에서도 안정적으로 동작하며, 작성 중이던 내용을 포함한 모든 데이터가 유실되지 않는다.

### 2.2 Goal Completion State
- 사용자가 파일을 열면 즉시 타이머를 시작/일시정지/리셋할 수 있는 상태
- 집중 세션이 정상 종료되면 완료 카운트가 증가하고 메모 입력 대기 상태로 전환되며, 사용자가 메모를 제출하거나 건너뛰면 다음 세션(짧은 휴식/긴 휴식)의 타이머가 자동으로 시작되는 상태 (단, 이 자동 시작은 사용자가 실시간으로 앱을 사용 중인 경우에 한한다 — BR-02)
- 짧은 휴식·긴 휴식이 정상 종료되면 별도 입력 없이 다음 Focus 세션이 자동으로 시작되는 상태 (동일하게 BR-02 적용)
- 로그 화면에서 하루 단위 완료 개수와 메모 목록을 확인할 수 있는 상태
- 설정 화면에서 집중/짧은휴식/긴휴식 시간을 변경하면, Idle 상태인 세션에는 즉시 반영되고 Running/Paused 상태인 세션에는 해당 세션 종료 이후부터 반영된 상태 (FR-07)
- 일기 화면에서 작성된 일기 전체가 날짜순 목록으로 보이고, 목록의 항목을 선택하면 그 내용을 열람·수정할 수 있으며, '새 일기' 조작으로 언제든 새 일기를 작성할 수 있는 상태 (FR-09 ~ FR-11)
- 일기를 작성·수정하는 도중 타이머 세션 종료 등 다른 이벤트가 발생해도 작성 중인 내용이 화면에서 사라지지 않으며, 저장하지 않은 채 새로고침하거나 브라우저를 재시작해도 작성 중이던 내용이 복원되는 상태 (FR-12, BR-06)
- 사용자가 타이머 화면이 아닌 다른 화면에 머무는 동안 Focus 세션이 종료되어도, 화면 이동 없이 메모 입력 대기 발생 사실을 인지할 수 있는 상태 (FR-13)
- 앱이 다시 실행되는 시점(새로고침·재접속)을 기준으로 진행 중이던 타이머, 설정값, 로그, 일기, 작성 중 초안이 정상 복원된 상태

### 2.3 Overall Success Conditions
- 인터넷 연결 없이 파일을 여는 것만으로 모든 기능이 정상 동작한다.
- 브라우저가 실행 중인 동안, 수 시간 이상 타이머를 켜두어도 표시 시간과 실제 경과 시간 간 오차(밀림)가 발생하지 않는다.
- 브라우저를 껐다 켜거나 새로고침해도, 앱이 다시 실행되는 시점을 기준으로 설정·로그·일기·작성 중 초안·진행 중이던 세션 상태가 정상 복원된다.
- 단, 브라우저가 완전히 종료되어 있던 동안 만료된 세션의 알림은 실시간으로 발생할 수 없으며, 앱이 다시 실행되는 시점에 종료 처리된다(EC-03 참고). 이 경우 다음 세션은 자동 시작되지 않고 Idle로 대기한다(BR-02).
- localStorage 쓰기가 실패하는 동안에도 앱의 사용 자체는 중단되지 않으며, 사용자는 저장 실패 사실을 인지할 수 있다(EC-05).
- 일기를 작성하는 도중 어떤 내부 화면 갱신이 발생하더라도 작성 중인 본문이 임의로 지워지거나 이전 값으로 되돌아가지 않는다(BR-06).

---

## 3. Scope

### 3.1 In Scope
- 25분 집중 타이머 시작/일시정지/리셋 (기본값: 집중 25분 / 짧은휴식 5분 / 긴휴식 15분)
- 집중 세션 종료 시 소리 및 브라우저 알림(권한 허용 시 동시 발송)
- 뽀모도로 사이클 자동 전환 (Focus ↔ Short Break, 4번째 Focus 슬롯 소모 후 Long Break)
- 현재 세션을 건너뛰고 다음 세션으로 즉시 이동하는 스킵 버튼
- 집중 세션 종료 시 한 줄 작업 메모 입력(빈 값·건너뛰기 허용)
- 하루 단위 완료 뽀모도로 개수 및 메모 목록(오름차순)을 보여주는 로그 화면
- 설정 화면에서 집중/짧은휴식/긴휴식 시간 직접 변경(1~180분 정수, 유효성 검증 포함)
- **일기 화면 신설**: 상단 주요 메뉴에 '일기' 항목을 추가하고, 목록 영역과 편집 영역이 함께 보이는 2단 구성으로 제공
- **일기 목록**: 저장된 모든 일기를 날짜 기준 최신순으로 나열하고, 항목 선택 시 편집 영역에 해당 일기를 표시
- **새 일기 작성**: '새 일기' 조작으로 빈 편집 상태에 진입하여 본문을 작성하고 명시적 저장 조작으로 확정. 같은 날짜에 여러 편 작성 가능
- **기존 일기 수정**: 목록에서 선택한 일기의 본문을 수정하고 다시 저장
- **일기 본문 글자 수 상한(5,000자) 및 남은 글자 수 표시**
- **작성 중 초안의 자동 보관 및 복원**: 저장하지 않은 작성 중 내용을 별도로 보관하여, 화면 갱신·새로고침·브라우저 재시작 후에도 복원
- **편집 중 입력 보호**: 사용자가 일기 본문을 편집하는 동안에는 화면 재갱신이 해당 입력 내용을 덮어쓰지 않음
- **화면 밖 세션 종료 인지 표시**: `Memo-Input-Pending` 상태일 때 상단 타이머 메뉴 항목에 시각적 표시를 부여
- 새로고침·재접속 시에도 유지되는 데이터 영속성 및 저장 실패 시 사용자 고지
- 완전 오프라인 동작 (단일 HTML 파일, 외부 리소스 미사용)
- 장시간 구동 시에도 시간 오차 없는 타이머 정확도(시스템 시계 정상 유지 조건 하)
- 지원 브라우저: Chrome/Edge/Firefox 최신 데스크톱 버전(`file://` 실행 기준)
- 세부 디자인 및 UI는 Development AI 재량으로 결정

### 3.2 Out of Scope / Non-goals
- Chrome, Edge, Firefox 최신 데스크톱 버전 외의 브라우저(Safari 등)에서의 정상 동작 보장
- 브라우저/OS 설정으로 인해 Notification API가 실제 화면에 알림을 표시하지 않는 경우의 감지·보정 (JS로 감지 불가능한 영역 — FR-02 참고)
- **앱 내에서의 일기 삭제 기능** (§7.5의 기존 원칙을 일기에도 동일하게 적용하며, 이번 개발 범위에서 제외한다)
- **일기 제목 필드** (일기는 본문만으로 구성하며, 목록 표시는 본문 앞부분을 사용한다)
- **일기의 날짜 직접 지정 및 변경** (일기의 날짜는 최초 작성 시점의 로컬 날짜로 자동 부여되며, 사용자가 과거·미래 날짜를 지정하거나 이후 변경할 수 없다)
- **일기 검색·태그·분류·서식(굵게·목록 등) 기능**

---

## 4. Functional Requirements

### FR-01. 타이머 시작/일시정지/리셋

**Purpose / Rationale**
- 사용자가 집중/휴식 세션의 진행을 직접 제어할 수 있도록 한다.

**Trigger**
- 사용자가 시작/일시정지/리셋 버튼을 클릭

**Input**
- 버튼 클릭 이벤트, 현재 세션 타입 및 설정된 세션 길이

**Processing / Business Rules**
- 시작: 현재 시각 기준으로 종료 목표 시각(endTimestamp = 현재시각 + 남은시간)을 계산하여 저장하고 세션 상태를 Running으로 전환
- 일시정지: 남은 시간을 고정하여 저장하고 세션 상태를 Paused로 전환
- 리셋: 현재 세션의 타이머만 설정된 길이의 Idle 상태로 되돌린다. 사이클 내 소모된 Focus 슬롯 수는 리셋의 영향을 받지 않고 그대로 유지된다(BR-03).
- 시작/일시정지/리셋 버튼은 Focus/Short Break/Long Break 세션이 Idle, Running, Paused 상태일 때만 노출된다. `Memo-Input-Pending` 상태에서는 이 버튼들이 노출되지 않는다(BR-04).

**Output**
- 화면에 갱신된 남은 시간 표시, 버튼 상태(시작/일시정지) 전환

**Expected State**
- 타이머가 사용자의 조작에 따라 Running/Paused/Idle 상태 중 하나로 정확히 반영되며, 사이클 진행도(소모된 Focus 슬롯 수)는 리셋과 무관하게 보존됨

**Acceptance Criteria**
- 시작 후 임의 시점에 표시되는 남은 시간이 실제 경과 시간과 일치한다.
- 일시정지 후 남은 시간이 변하지 않는다.
- 리셋 시 설정된 세션 길이의 Idle 상태로 정확히 복귀한다.
- 3번째 Focus 슬롯까지 소모된 상태에서 4번째 Focus를 리셋해도, 슬롯 소모 수는 3으로 유지된다.
- `Memo-Input-Pending` 상태에서는 시작/일시정지/리셋 버튼이 화면에 나타나지 않는다.

---

### FR-02. 세션 종료 알림

**Purpose / Rationale**
- 집중/휴식 세션이 끝났음을 사용자에게 즉시 알려 흐름이 끊기지 않게 한다.

**Trigger**
- 진행 중인 세션의 남은 시간이 0에 도달

**Input**
- 타이머의 종료 목표 시각과 현재 시각의 비교 결과

**Processing / Business Rules**
- Notification 권한이 허용된 경우, 소리 알림(Web Audio API 합성음)과 브라우저 알림(Notification API)을 항상 동시에 발송한다(택일이 아님).
- Notification 권한이 거부되었거나 Notification API 자체를 지원하지 않는 경우, 소리 알림과 탭 제목 변경으로 대체한다(EC-01).
- **소리 알림은 브라우저 알림 권한 상태와 무관하게 항상 재생을 시도하는 주(主) 알림 수단이다.** Notification API 호출이 성공(권한 허용, 표시 함수 정상 호출)해도 OS/브라우저 설정에 의해 실제 화면에 알림이 표시되지 않을 수 있으나, 이는 JavaScript로 감지할 수 없는 영역이므로 별도 감지·보정을 하지 않는다(3.2). 반면 오디오 재생 실패는 `play()` 호출의 Promise 거부로 감지 가능하므로, 실패 시 탭 제목 변경으로 즉시 대체한다.
- 브라우저의 오디오 자동재생 제한을 회피하기 위해, 사용자가 앱과 최초로 상호작용(예: 시작 버튼 클릭)하는 시점에 오디오 컨텍스트를 활성화한다(세부 구현은 12.3에 따라 Development AI 재량)

**Output**
- 알림음 재생, (권한 허용 시) 데스크톱 알림 표시 시도, (권한 거부/미지원 시) 탭 제목 변경

**Expected State**
- 앱이 실행 중인 동안, 사용자가 다른 작업 중이어도 세션 종료 사실을 인지할 수 있는 상태

**Acceptance Criteria**
- 앱이 실행 중인 동안에는 세션 종료 시점에 알림이 지체 없이 발생한다.
- 권한이 허용된 경우 소리 재생과 브라우저 알림 호출이 함께 발생한다.
- 오디오 재생이 실패로 감지되면 탭 제목 변경으로 대체된다.

---

### FR-03. 뽀모도로 사이클 자동 전환

**Purpose / Rationale**
- 뽀모도로 기법의 집중-휴식 리듬을 사용자 개입 없이 자동으로 유지한다.

**Trigger**
- 진행 중이던 세션이 정상 종료(시간 만료)되었을 때

**Input**
- 현재 세션 타입, 사이클 내 소모된 Focus 슬롯 수

**Processing / Business Rules**
- Focus 세션이 정상 종료되면: (1) 완료 카운트를 1 증가 → (2) 세션 상태를 `Focus 완료 + Memo-Input-Pending`으로 전환(이 상태에서는 다음 세션의 타이머가 시작되지 않음) → (3) 사용자가 메모를 제출하거나 건너뛰면 BR-01 순서에 따라 다음 세션이 결정되어 자동으로 카운트다운을 시작
- Short Break 또는 Long Break가 정상 종료되면: 메모 절차 없이 곧바로 다음 Focus 세션이 자동으로 카운트다운을 시작
- 완료 카운트 증가와 Focus 슬롯 소모 처리 간의 저장 순서 등 내부 데이터 저장 세부사항은 12.3 Implementation Freedom에 따라 Development AI가 결정한다.

**Output**
- 다음 세션 타입과 길이로 초기화되어 자동 시작된 타이머 화면

**Expected State**
- 사용자 개입 없이 BR-01 규칙에 따라 정확한 다음 세션으로 자동 진입한 상태

**Acceptance Criteria**
- 4번째 Focus 슬롯이 소모된 직후에는 반드시 Long Break로 전환된다.
- 그 외 Focus 종료 시에는 Short Break로 전환된다.
- Break 세션 종료 후에는 메모 입력 없이 곧바로 다음 Focus가 시작된다.

---

### FR-04. 세션 건너뛰기 (Skip)

**Purpose / Rationale**
- 사용자가 현재 세션을 즉시 종료하고 다음 세션으로 넘어갈 수 있도록 한다.

**Trigger**
- 사용자가 스킵 버튼 클릭

**Input**
- 버튼 클릭 이벤트, 현재 세션 타입

**Processing / Business Rules**
- 현재 세션을 미완료 상태로 즉시 종료하고 BR-01 순서에 따라 다음 세션으로 자동 전환·시작
- 스킵된 세션이 Focus였던 경우, 완료 카운트를 증가시키지 않고 메모 입력도 요구하지 않는다. 다만 사이클 내 Focus 슬롯은 소모된 것으로 계산되어 Long Break 도달 순서에는 정상 반영된다.
- 스킵 버튼은 Focus/Short Break/Long Break가 Idle, Running, Paused 상태일 때만 노출된다. `Memo-Input-Pending` 상태에서는 스킵 버튼이 노출되지 않는다(BR-04) — 이 상태는 메모 제출/건너뛰기로만 벗어날 수 있다.

**Output**
- 다음 세션 타입과 길이로 초기화되어 자동 시작된 타이머 화면

**Expected State**
- 스킵된 세션은 로그에 기록되지 않되, 사이클 순서(슬롯 소모)는 정상 진행된 상태

**Acceptance Criteria**
- 스킵 직후 로그의 완료 개수가 변하지 않는다(Focus 스킵 시).
- Focus를 2회 스킵한 뒤 정상 완료를 2회 채우면, 총 4개의 Focus 슬롯이 소모되어 Long Break로 전환된다(완료 카운트는 2로 기록됨).
- `Memo-Input-Pending` 상태에서는 스킵 버튼이 나타나지 않는다.

---

### FR-05. 작업 메모 기록

**Purpose / Rationale**
- 각 집중 세션에서 수행한 작업을 짧게 기록하여 이후 조회할 수 있게 한다.

**Trigger**
- Focus 세션이 정상 종료(시간 만료)되었을 때

**Input**
- 사용자가 입력하는 한 줄 텍스트(제출 또는 건너뛰기)

**Processing / Business Rules**
- 사용자는 메모를 제출하거나 건너뛸 수 있다. 빈 값(공백 또는 미입력) 제출과 건너뛰기 모두 허용되며, 두 경우 모두 완료 카운트는 정상 반영되고 메모 텍스트만 빈 값으로 저장된다. 별도의 '취소' 동작은 없다(제출 또는 건너뛰기 중 하나로만 대기 상태를 벗어남).
- 저장된 메모는 완료 시각과 함께 해당 날짜의 로그 데이터에 추가된다.
- 작업 메모는 일기(FR-09 ~ FR-12)와 별개의 데이터이며, 서로의 내용에 영향을 주지 않는다.

**Output**
- 로그 화면에 반영되는 메모 항목(빈 값일 경우 '메모 없음'으로 표시)

**Expected State**
- 해당 Focus 세션의 완료 기록과 메모(또는 빈 메모)가 하루 단위 로그에 귀속된 상태

**Acceptance Criteria**
- Focus 세션이 정상 종료될 때마다 메모 입력 창이 표시된다.
- 빈 값 제출 또는 건너뛰기를 선택해도 완료 카운트는 정상 증가한다.

---

### FR-06. 일별 로그 화면

**Purpose / Rationale**
- 사용자가 하루 동안의 생산성(완료한 뽀모도로 개수)과 수행 내역(메모)을 한눈에 파악할 수 있도록 한다.

**Trigger**
- 사용자가 로그 화면으로 이동

**Input**
- 저장된 날짜별 로그 데이터

**Processing / Business Rules**
- 선택된 날짜(기본값: 오늘)의 완료된 뽀모도로 개수와 메모 목록을 오름차순(시간순, 오래된 순)으로 정리하여 표시한다.
- 모든 시각은 사용자 기기의 로컬 타임존을 기준으로 한다.

**Output**
- 날짜별 완료 개수 및 메모 리스트 UI

**Expected State**
- 로그 화면이 항상 실제 저장된 데이터와 일치하는 상태

**Acceptance Criteria**
- 표시되는 완료 개수가 해당 날짜에 정상 종료된 Focus 세션 수와 정확히 일치한다.
- 메모가 오래된 순으로 정렬되어 표시된다.

---

### FR-07. 설정 (세션 길이 커스터마이징)

**Purpose / Rationale**
- 사용자가 자신의 작업 스타일에 맞게 세션 길이를 조정할 수 있도록 한다.

**Trigger**
- 사용자가 설정 화면에서 값을 변경하고 저장

**Input**
- 집중시간/짧은휴식/긴휴식 각각의 사용자 입력 값(분 단위)

**Processing / Business Rules**
- 기본값은 집중 25분 / 짧은휴식 5분 / 긴휴식 15분이다.
- 입력값은 1분 이상 180분 이하의 정수만 허용하며, 범위를 벗어나거나 소수·비숫자가 입력되면 저장을 막고 오류를 표시한다.
- **적용 시점**: 설정 저장 시점에 해당 세션 타입이 **Idle 상태**라면(아직 시작되지 않았으므로) 화면 표시와 다음 시작 시 사용할 길이가 **즉시** 새 값으로 갱신된다. 해당 세션 타입이 **Running 또는 Paused 상태**라면 변경된 값은 그 세션에는 적용되지 않고, 해당 세션이 종료된 이후 같은 타입의 세션부터 적용된다.

**Output**
- 갱신된 설정값이 반영된 타이머 화면(Idle 세션은 즉시, 진행 중 세션은 다음 회차부터)

**Expected State**
- Idle 상태의 세션은 새 설정값을 즉시 표시하고, Running/Paused 상태의 세션은 기존 길이를 유지한 채 종료 후부터 새 값을 따르는 상태

**Acceptance Criteria**
- 설정 화면에서 값을 변경하고 저장하면, 새로고침 후에도 변경된 값이 유지된다.
- 범위를 벗어난 값(0 이하, 181 이상, 소수, 비숫자)은 저장되지 않는다.
- 현재 Focus가 Idle 상태에서 설정을 변경하면, 화면의 Focus 남은시간 표시가 즉시 새 값으로 바뀐다.
- 현재 Focus가 Running 또는 Paused 상태에서 설정을 변경하면, 그 세션은 기존 길이로 계속 진행되고 다음 Focus 세션부터 새 값이 적용된다.

---

### FR-08. 데이터 영속성 및 상태 복원

**Purpose / Rationale**
- 새로고침이나 브라우저 재시작 후에도 사용자의 진행 상황과 데이터를 잃지 않도록 한다.

**Trigger**
- 페이지 로드(새로고침 또는 재접속)

**Input**
- localStorage에 저장된 설정 / 로그 / 진행 중 타이머 상태 / 일기 / 작성 중 초안 데이터

**Processing / Business Rules**
- 저장된 설정값, 로그, 일기 목록, 작성 중 초안, 진행 중이던 타이머의 세션 타입·세션 상태·사이클 내 소모된 Focus 슬롯 수를 그대로 복원한다.
- **Running 상태로 저장된 세션**: 저장된 종료 목표 시각(endTimestamp)과 현재 시각을 비교하여 만료 여부를 판단한다. 이미 종료된 경우, 오프라인 기간 동안 몇 번의 세션이 경과했는지와 무관하게 '중단 시점의 세션 1회만' 종료 처리한 뒤 다음 세션을 Idle 상태로 대기시킨다(사용자가 직접 시작, BR-02). 아직 남아 있다면 남은 시간을 그대로 이어서 표시한다.
- **Paused 상태로 저장된 세션**: `endTimestamp`가 존재하지 않으므로 만료 여부를 판단하지 않는다. 저장된 남은 시간 스냅샷과 Paused 상태를 그대로 복원하며, 오프라인 기간이 얼마였든 남은 시간은 변하지 않는다(일시정지 중에는 시간이 흐르지 않으므로).
- 완료 처리된 세션의 로그 귀속 날짜는 원래의 종료 목표 시각(endTimestamp) 기준 날짜로 기록한다.
- 일기와 작성 중 초안의 복원 규칙은 각각 FR-09, FR-12에 정의한다.

**Output**
- 복원된 타이머 화면, 설정 화면, 로그 화면, 일기 화면

**Expected State**
- 새로고침/재시작 전후 사용자 관점에서 데이터 손실이나 진행 상황 단절이 없는 상태

**Acceptance Criteria**
- 임의 시점에 새로고침해도 타이머 진행 상태(남은 시간 포함), 설정값, 로그, 일기 목록이 새로고침 이전과 동일하게 유지된다.
- 여러 세션이 경과한 뒤 재접속해도 완료 처리는 1회로 제한되고, 그 세션의 로그는 원래 종료 목표 시각 기준 날짜에 기록된다.
- 복원 후 다음 세션은 자동으로 시작되지 않고 Idle 상태로 대기하며, 사용자의 시작 조작을 필요로 한다.
- Paused 상태에서 새로고침한 경우, 오프라인 시간과 무관하게 남은 시간 스냅샷이 그대로 복원되며 종료 처리되지 않는다.

---

### FR-09. 일기 화면 및 일기 목록 조회

**Purpose / Rationale**
- 사용자가 작성한 일기 전체를 한 화면에서 시간 흐름에 따라 훑어보고, 원하는 항목을 즉시 열어볼 수 있도록 한다.

**Trigger**
- 사용자가 상단 주요 메뉴에서 '일기' 항목을 선택하여 일기 화면으로 이동

**Input**
- localStorage에 저장된 일기 목록 데이터

**Processing / Business Rules**
- 일기 화면은 **목록 영역**과 **편집 영역**을 함께 배치한 2단 구성으로 제공한다. 화면 폭이 좁은 환경에서는 두 영역이 위아래로 배치될 수 있으며, 배치 전환의 구체적 기준과 방식은 12.3에 따라 Development AI가 결정한다.
- 목록 영역은 저장된 **모든 일기**를 BR-05의 정렬 규칙(날짜 기준 최신 우선)에 따라 나열한다.
- 목록의 각 항목은 해당 일기의 **날짜**와 **본문 앞부분 미리보기**를 표시한다. 본문이 비어 있지 않은 한 미리보기는 본문의 앞부분에서 취하며, 미리보기로 표시할 글자 수와 말줄임 처리 방식은 12.3에 따라 Development AI가 결정한다.
- 목록 영역 상단에는 **'새 일기' 조작 요소**를 항상 노출한다(FR-10).
- 저장된 일기가 하나도 없는 경우, 목록 영역에 비어 있음을 알리는 안내 문구를 표시한다.
- 목록의 항목을 선택하면 해당 일기가 편집 영역에 열린다(FR-11).
- 페이지 로드 시 저장된 일기 데이터를 읽어들이며, 개별 항목이 BR-05가 정의한 필수 속성(고유 식별자, 날짜, 본문, 생성 시각, 최종 수정 시각)을 갖추지 못했거나 형식이 유효하지 않은 경우 해당 항목만 제외하고 나머지를 정상 복원한다. 이 제외 처리로 인해 앱 실행이 중단되지 않는다.
- 일기 화면에 진입하는 시점에 편집 영역에 무엇을 표시할지는 FR-12의 초안 복원 규칙을 우선 적용하고, 복원할 초안이 없으면 편집 영역을 빈 상태로 둔다.

**Output**
- 날짜 기준 최신순으로 정렬된 일기 목록 UI, 선택된 일기가 열린 편집 영역, '새 일기' 조작 요소

**Expected State**
- 화면에 표시되는 일기 목록이 항상 저장된 일기 데이터와 일치하고, 정렬 순서가 BR-05를 따르는 상태

**Acceptance Criteria**
- 일기 화면에 저장된 모든 일기가 빠짐없이 목록에 나타난다.
- 목록이 날짜 기준 최신 일기부터 순서대로 표시된다.
- 저장된 일기가 없을 때 안내 문구가 표시되고, 목록 항목은 하나도 표시되지 않는다.
- 목록의 항목을 선택하면 그 일기의 본문이 편집 영역에 표시된다.
- 새로고침 후에도 목록의 내용과 순서가 동일하게 유지된다.

---

### FR-10. 새 일기 작성 및 저장

**Purpose / Rationale**
- 사용자가 원하는 시점에 자유 분량의 기록을 남기고, 명시적인 조작으로 그 내용을 확정할 수 있도록 한다.

**Trigger**
- 사용자가 '새 일기' 조작 요소를 선택하여 새 일기 작성을 시작하고, 본문을 입력한 뒤 저장 조작을 수행

**Input**
- 사용자가 편집 영역에 입력한 본문 텍스트

**Processing / Business Rules**
- '새 일기'를 선택하면 편집 영역이 **빈 작성 상태**로 전환되며, 이전에 열려 있던 일기의 내용은 편집 영역에서 해제된다. 이때 아직 저장하지 않은 작성 중 내용이 있었다면 FR-12의 초안 보관 규칙에 따라 처리된다.
- 일기는 **본문만으로 구성**되며 제목 필드를 두지 않는다.
- 본문 길이는 **최대 5,000자**로 제한한다. 상한 초과 시의 처리는 EC-06에 따른다.
- 편집 영역에는 현재 입력된 글자 수와 상한을 함께 표시하여 사용자가 남은 분량을 인지할 수 있게 한다.
- 저장은 **명시적인 저장 조작으로만** 이루어진다. 저장 조작 없이 화면을 이동하거나 앱을 종료해도 일기 목록에는 추가되지 않으며, 그 내용은 FR-12의 초안으로만 보관된다.
- **빈 본문(공백만 입력한 경우 포함)은 저장할 수 없다.** 이 경우 저장을 수행하지 않고 사용자에게 본문이 필요함을 알린다.
- 저장이 수행되면 새 일기 항목이 생성되어 일기 목록에 추가된다. 이때 부여되는 값은 BR-05를 따른다.
  - 고유 식별자: BR-05의 생성 규칙에 따라 부여
  - 날짜: **저장 시점의 사용자 로컬 날짜**로 자동 부여하며, 사용자가 지정하거나 이후 변경할 수 없다
  - 본문: 입력된 텍스트(앞뒤 공백은 제거하여 저장)
  - 생성 시각 및 최종 수정 시각: 저장 시점의 시각
- **같은 날짜에 여러 편의 일기를 저장할 수 있다.** 같은 날짜의 기존 일기는 덮어쓰지 않으며, 별개의 항목으로 추가된다.
- 저장이 완료되면 해당 일기는 편집 영역에서 '저장된 일기를 열람·수정 중인 상태'로 전환되고(FR-11), 보관 중이던 초안은 해제된다(FR-12).
- 저장 결과는 localStorage에 반영되며, 쓰기 실패 시의 처리는 EC-05에 따른다.

**Output**
- 일기 목록에 추가된 새 항목, 저장 완료 사실을 알리는 표시, 갱신된 편집 영역 상태

**Expected State**
- 저장된 일기가 목록과 저장소 양쪽에 반영되어 있고, 편집 영역은 방금 저장한 일기를 열람·수정 중인 상태

**Acceptance Criteria**
- '새 일기' 선택 시 편집 영역이 빈 상태가 된다.
- 본문을 입력하고 저장하면 목록 최상단(BR-05 정렬 기준상 최신 위치)에 새 항목이 나타난다.
- 같은 날짜에 두 편을 저장하면 두 항목이 모두 목록에 존재하며, 앞서 저장한 일기의 본문이 변경되지 않는다.
- 본문이 비어 있거나 공백만 있는 상태에서 저장을 시도하면 항목이 추가되지 않고 안내가 표시된다.
- 저장 후 새로고침해도 저장된 일기가 그대로 남아 있다.
- 저장하지 않은 채 다른 화면으로 이동하면 목록에는 새 항목이 추가되지 않는다.

---

### FR-11. 기존 일기 열람 및 수정

**Purpose / Rationale**
- 이미 작성한 일기의 내용을 다시 확인하고, 잘못 쓴 부분이나 덧붙일 내용을 반영할 수 있도록 한다.

**Trigger**
- 사용자가 일기 목록에서 항목을 선택하고, 편집 영역에서 본문을 수정한 뒤 저장 조작을 수행

**Input**
- 선택된 일기의 고유 식별자, 사용자가 수정한 본문 텍스트

**Processing / Business Rules**
- 목록의 항목을 선택하면 해당 일기의 본문이 편집 영역에 그대로 표시되고, 편집 가능한 상태가 된다.
- 편집 영역은 현재 열람·수정 중인 일기의 **날짜**를 함께 표시한다. 이 날짜는 표시 전용이며 수정할 수 없다(3.2).
- 본문 수정에는 FR-10과 동일한 5,000자 상한과 빈 본문 저장 금지 규칙이 적용된다.
- 저장 조작을 수행하면 해당 고유 식별자를 가진 일기의 **본문과 최종 수정 시각만** 갱신되며, 고유 식별자·날짜·생성 시각은 변경되지 않는다.
- 저장하지 않은 수정 내용은 FR-12의 초안 보관 규칙을 따르며, 저장 전까지 원본 일기 데이터에는 반영되지 않는다.
- 다른 항목을 선택하거나 '새 일기'를 선택하면 편집 대상이 전환되며, 이때 저장하지 않은 수정 내용의 처리는 FR-12를 따른다.
- 앱 내에서 일기를 삭제하는 기능은 제공하지 않는다(3.2, 7.5).

**Output**
- 편집 영역에 표시된 기존 일기 본문, 저장 후 갱신된 목록 미리보기, 저장 완료 표시

**Expected State**
- 수정된 본문이 저장소와 목록에 반영되어 있고, 해당 일기의 고유 식별자·날짜·생성 시각은 최초 저장 당시 값을 유지한 상태

**Acceptance Criteria**
- 목록에서 선택한 일기의 본문이 편집 영역에 정확히 표시된다.
- 본문을 수정하고 저장하면 목록의 미리보기가 수정된 내용을 반영한다.
- 수정 후에도 해당 일기의 날짜가 변경되지 않는다.
- 수정 후 새로고침해도 수정된 본문이 유지된다.
- 수정 저장으로 인해 일기 항목의 개수가 증가하지 않는다.

---

### FR-12. 작성 중 초안의 자동 보관 및 복원

**Purpose / Rationale**
- 일기는 작성에 긴 시간이 소요될 수 있으므로, 저장 조작 이전에 발생하는 화면 갱신·새로고침·브라우저 종료로 작성 중인 내용이 유실되지 않도록 한다.

**Trigger**
- 사용자가 편집 영역에 본문을 입력·수정하는 동안, 그리고 페이지 로드 시점

**Input**
- 편집 영역의 현재 본문 텍스트, 현재 편집 대상(새 일기인지, 기존 일기라면 그 고유 식별자)

**Processing / Business Rules**
- 저장되지 않은 편집 상태를 **초안(Draft)** 이라 하며, 초안은 일기 목록과는 **별개의 데이터**로 localStorage에 보관한다. 초안은 동시에 하나만 존재한다.
- 초안에는 현재 본문 텍스트와 편집 대상 정보(새 일기 작성 중인지, 기존 일기 수정 중인지 및 그 고유 식별자)를 함께 보관한다.
- 초안 보관은 **입력이 멈춘 뒤 일정 시간이 지난 시점에 1회 수행**한다. 매 글자 입력마다 저장하지 않는다. 구체적인 대기 시간은 12.3에 따라 Development AI가 결정한다.
- 페이지 로드 시 보관된 초안이 존재하면, 사용자가 일기 화면에 진입했을 때 편집 영역에 그 내용을 복원하고 **저장되지 않은 작성 중 내용임을 알리는 표시**를 함께 노출한다.
- 초안이 기존 일기를 수정 중이던 것이라면 해당 일기를 편집 대상으로 복원한다. 복원 시점에 그 고유 식별자를 가진 일기가 존재하지 않으면, 초안 본문을 유지한 채 **새 일기 작성 상태로 전환**하여 내용이 유실되지 않게 한다.
- 저장 조작이 성공하면 보관 중이던 초안을 해제한다.
- 편집 대상이 전환될 때(다른 항목 선택 또는 '새 일기' 선택) 직전까지의 편집 내용은 초안으로 보관되지 않고 해제되며, 새 편집 대상의 내용이 편집 영역에 표시된다. 즉 초안은 항상 **가장 최근에 편집 중이던 하나**만을 가리킨다.
- 초안 데이터에도 5,000자 상한이 동일하게 적용된다.
- 초안 저장 실패 시의 처리는 EC-05에 따른다.

**Output**
- localStorage에 보관된 초안 데이터, 페이지 재진입 시 복원된 편집 영역과 미저장 상태 표시

**Expected State**
- 저장 조작 없이 앱이 종료되거나 새로고침되어도, 다시 일기 화면에 진입했을 때 작성 중이던 본문이 편집 영역에 남아 있는 상태

**Acceptance Criteria**
- 본문을 입력한 뒤 저장하지 않고 새로고침해도, 일기 화면의 편집 영역에 입력하던 본문이 복원된다.
- 복원된 내용에는 저장되지 않은 상태임을 알리는 표시가 함께 나타난다.
- 초안이 복원된 상태에서는 일기 목록에 해당 내용의 항목이 존재하지 않는다.
- 저장 조작을 완료한 뒤 새로고침하면, 초안이 아니라 저장된 일기로 목록에 나타나고 미저장 표시는 사라진다.
- 기존 일기를 수정하던 중 저장하지 않고 새로고침하면, 그 일기를 수정 중이던 상태와 수정 중이던 본문이 함께 복원된다.

---

### FR-13. 타이머 화면 밖에서의 세션 종료 인지 표시

**Purpose / Rationale**
- 일기 화면처럼 오래 머무는 화면에서는 Focus 세션이 종료되어 메모 입력 대기 상태가 되어도 사용자가 이를 인지하지 못한 채 방치할 수 있다. 화면을 이동하지 않고도 그 사실을 알 수 있게 한다.

**Trigger**
- 시스템이 `Memo-Input-Pending` 상태로 진입하거나 그 상태에서 벗어날 때

**Input**
- 현재 `Memo-Input-Pending` 상태 여부

**Processing / Business Rules**
- `Memo-Input-Pending` 상태인 동안, 상단 주요 메뉴의 **타이머 항목에 시각적 표시**를 부여하여 확인이 필요함을 알린다. 표시의 구체적 형태(점, 배지 등)와 스타일은 12.3에 따라 Development AI가 결정한다.
- 이 표시는 사용자가 현재 어느 화면에 있든 상단 메뉴에 노출된다.
- 메모 제출 또는 메모 건너뛰기로 `Memo-Input-Pending` 상태에서 벗어나면 표시를 즉시 해제한다.
- 이 표시는 알림 수단을 추가하는 것이 아니라 기존 상태를 화면에 드러내는 것이므로, FR-02의 소리·브라우저 알림 동작에는 영향을 주지 않는다.
- 이 표시는 사용자의 조작을 막지 않는다. 사용자는 표시가 있는 상태에서도 일기 작성을 포함한 다른 화면의 조작을 계속할 수 있다.

**Output**
- 상단 타이머 메뉴 항목에 부여된 시각적 표시

**Expected State**
- `Memo-Input-Pending` 상태 여부와 상단 메뉴의 표시 여부가 항상 일치하는 상태

**Acceptance Criteria**
- Focus 세션이 정상 종료되어 메모 입력 대기 상태가 되면 타이머 메뉴 항목에 표시가 나타난다.
- 일기 화면에 머무는 동안에도 그 표시를 볼 수 있다.
- 메모를 제출하거나 건너뛰면 표시가 사라진다.
- Focus가 아닌 휴식 세션이 종료된 경우에는 표시가 나타나지 않는다.

---

## 5. Business / System Rules

### BR-01. 세션 순서 및 완료 카운트 규칙
- 세션은 Focus → Short Break → Focus → Short Break → Focus → Short Break → Focus → Long Break 순서로 반복되며, Long Break 이후 다시 Focus부터 사이클이 재시작된다. Long Break가 스킵된 경우에도 동일하게 사이클이 초기화되어 Focus부터 재시작된다.
- 사이클 내 Focus 슬롯(1~4번째)은 완료·스킵 여부와 무관하게 소모되며, 4번째 Focus 슬롯이 소모되면 Long Break로 전환된다.
- 하루 단위 완료 개수(로그 표시용)는 정상적으로(시간 만료로) 종료된 Focus 세션에 대해서만 1씩 증가한다. 스킵된 Focus는 완료 개수에 포함되지 않는다.
- 스킵 버튼은 Focus, Short Break, Long Break 모든 세션 타입에서 사용 가능하다(단, BR-04에 따라 `Memo-Input-Pending` 상태는 제외).

### BR-02. 자동 시작의 적용 범위
- 세션 종료 후 다음 세션의 자동 시작(FR-03)은 **사용자가 실시간으로 앱을 사용 중인 상황에서만** 적용되는 원칙이다.
- 앱이 장시간 비활성 상태였다가 재개된 경우 — 즉 새로고침·재접속으로 인해 이미 만료된 세션을 복원하는 경우(FR-08, EC-03) — 에는 다음 세션을 자동으로 시작하지 않고 Idle 상태로 대기시켜, 사용자가 실제로 자리에 복귀했음을 스스로 확인한 뒤 다음 세션을 직접 시작하도록 한다.
- 이는 사용자가 인지하지 못한 채로 세션(특히 Focus)이 자동 진행되어 소모되는 것을 방지하기 위함이다.

### BR-03. 리셋(Reset)과 사이클 슬롯의 독립성
- 리셋(FR-01)은 현재 진행 중인 세션의 타이머만 초기화하며, 사이클 내 소모된 Focus 슬롯 수(사이클 진행도)에는 영향을 주지 않는다.
- 즉, 리셋 후에도 사이클 진행 단계(몇 번째 Focus 슬롯까지 소모되었는지)는 리셋 이전 상태로 유지된다.

### BR-04. Memo-Input-Pending 상태의 조작 제한
- `Memo-Input-Pending` 상태에서는 타이머 제어 버튼(시작/일시정지/리셋/스킵)이 화면에 노출되지 않는다.
- 이 상태에서 노출되는 조작은 메모 제출과 메모 건너뛰기(FR-05) 두 가지뿐이며, 둘 중 하나를 수행해야만 이 상태를 벗어나 다음 세션으로 진입한다.
- 이는 다음 세션이 아직 결정·시작되지 않은 과도기 상태에서 사용자가 타이머를 조작해 상태 머신이 꼬이는 것을 방지하기 위함이다.
- 이 조작 제한은 타이머 화면에 한정되며, 사용자는 이 상태에서도 로그·설정·일기 화면으로 이동하고 해당 화면의 조작(일기 작성·저장 포함)을 수행할 수 있다. 이 상태가 지속되고 있다는 사실은 FR-13의 표시로 알린다.

### BR-05. 일기 데이터의 식별 및 정렬 규칙
- 일기 항목은 **날짜를 키로 하는 구조가 아니라, 각 항목이 고유 식별자를 갖는 목록 구조**로 관리한다. 이는 같은 날짜에 여러 편의 일기를 보관하기 위함이다.
- 각 일기 항목은 다음 속성을 필수로 가진다: **고유 식별자, 날짜, 본문, 생성 시각, 최종 수정 시각**.
- **고유 식별자 생성**: 생성 시점의 시각 값과 같은 시각에 생성된 항목을 구분하기 위한 순번(카운터)을 조합하여 만든다. 하나의 앱 실행 중은 물론 여러 번의 실행을 거쳐도 기존 항목과 중복되지 않아야 한다. 난수 생성 API에 의존하지 않는다.
- **날짜 부여**: 일기의 날짜는 최초 저장 시점의 사용자 로컬 날짜로 자동 부여되며, 이후 수정으로 변경되지 않는다. 사용자가 직접 지정할 수 없다.
- **정렬**: 일기 목록은 **날짜 기준 내림차순(최신 날짜가 위)** 으로 정렬한다. 날짜가 같은 항목이 둘 이상인 경우 **생성 시각 기준 내림차순(나중에 만든 것이 위)** 으로 정렬한다. 이 정렬 규칙은 저장 순서와 무관하게 화면 표시 시점에 항상 적용된다.
- 이 정렬 방향은 작업 로그 화면의 메모 정렬(오름차순, FR-06)과 다르며, 두 화면은 각자의 규칙을 독립적으로 따른다.
- 모든 날짜와 시각은 사용자 기기의 로컬 타임존을 기준으로 한다.

### BR-06. 편집 중 입력 보호 규칙
- 사용자가 일기 편집 영역에 입력하고 있는 동안에는, 앱 내부의 어떤 화면 갱신도 그 입력 내용을 저장된 값이나 이전 값으로 되돌려서는 안 된다.
- 이 규칙은 타이머의 주기적 화면 갱신, 세션 종료 처리, 시스템 시계 변경 감지, 설정 저장, 저장 실패 경고 표시 등 편집 영역과 직접 관련이 없는 모든 갱신 상황에 적용된다.
- 편집 영역의 내용이 바뀌는 경우는 다음 세 가지뿐이다: (1) 사용자가 직접 입력한 경우, (2) 사용자가 목록의 다른 항목이나 '새 일기'를 선택하여 편집 대상을 전환한 경우, (3) 페이지 로드 시 FR-12에 따라 초안이 복원된 경우.
- 이 규칙을 만족시키는 구현 방식은 12.3에 따라 Development AI가 결정한다.

---

## 6. State & Data Model

### 6.1 System / UI States
- Focus-Idle / Focus-Running / Focus-Paused
- Short Break-Idle / Short Break-Running / Short Break-Paused
- Long Break-Idle / Long Break-Running / Long Break-Paused
- Memo-Input-Pending (Focus 세션 정상 종료 후 메모 입력 대기 — 이 상태에서는 다음 세션의 타이머가 아직 시작되지 않으며, 타이머 제어 버튼도 노출되지 않는다. 메모 제출/건너뛰기 UI만 노출된다 — BR-04. 이 상태 동안 상단 타이머 메뉴에 표시가 부여된다 — FR-13)
- Settings (설정 화면)
- Log (로그 화면)
- Diary (일기 화면) — 아래 세 가지 편집 영역 상태 중 하나를 가진다
  - Diary-Empty: 편집 대상이 없는 빈 상태
  - Diary-New: 새 일기를 작성 중인 상태 (아직 저장되지 않음)
  - Diary-Editing: 저장된 일기를 열람·수정 중인 상태

### 6.2 Data States
- 진행 중 타이머 상태: **존재(Active)**는 TimerState의 세션 상태가 Running 또는 Paused인 경우를, **없음(None)**은 세션 상태가 Idle인 경우를 의미한다. 즉 Active/None은 6.1의 Running/Paused와 Idle을 각각 상위 개념으로 묶어 부르는 것이며 1:1로 대응한다.
- `Memo-Input-Pending`은 이전 Focus 세션이 이미 완료·종료되고 다음 세션은 아직 시작되지 않은 시점의 별도 UI 오버레이 상태다. 이 시점의 기저 TimerState 세션 상태는 Idle(즉, 데이터 상태는 None)이다.
- 설정값 상태: 기본값 / 사용자 변경값
- 일별 로그 상태: 데이터 없음(해당 날짜에 완료 기록 없음) / 데이터 존재
- 일기 목록 상태: 데이터 없음(저장된 일기가 하나도 없음) / 데이터 존재
- 초안 상태: 없음(보관된 초안 없음) / 존재(저장되지 않은 편집 내용 보관 중). 저장 조작 성공 또는 편집 대상 전환 시 '없음'으로 전이하고, 편집 중 입력 후 일정 시간 경과 시 '존재'로 전이한다.
- 저장소 상태: 정상(Synced) / 쓰기 실패(Write-Failed, EC-05)

### 6.3 Core Data Entities

**TimerState**
- 세션 타입 (Focus / ShortBreak / LongBreak)
- 세션 상태 (Idle / Running / Paused)
- 종료 목표 시각 (endTimestamp, Running 상태일 때만 유효, 로컬 타임존 기준)
- 남은 시간 스냅샷 (Paused 상태일 때만 유효 — 정의상 항상 1초 이상의 양수 값을 가진다. 남은 시간이 0에 도달하는 순간은 Running 상태에서만 발생하며, 그 즉시 세션 종료 처리로 전이하므로 Paused로는 진입하지 않는다)
- 사이클 내 소모된 Focus 슬롯 수 (0~4)
- 시계 이상 감지용 앵커: 마지막으로 갱신된 `dateAnchor`(Date.now() 값)와 `perfAnchor`(performance.now() 값) — EC-04 참고

**Settings**
- 집중시간 길이(분), 기본값 25
- 짧은휴식 길이(분), 기본값 5
- 긴휴식 길이(분), 기본값 15
- (공통) 허용 범위: 1~180분 정수

**DailyLog**
- 날짜 (로컬 타임존 기준)
- 완료된 뽀모도로 개수
- 메모 목록 (각 항목: 완료 시각, 메모 텍스트 — 빈 값 허용)

**DiaryEntry**
- 고유 식별자 — BR-05의 생성 규칙에 따라 부여되며, 항목을 식별하는 유일한 기준. 생성 후 변경되지 않는다.
- 날짜 (로컬 타임존 기준) — 최초 저장 시점의 날짜. 변경되지 않는다.
- 본문 — 최대 5,000자의 텍스트. 빈 값은 허용되지 않는다.
- 생성 시각 — 최초 저장 시점의 시각. 변경되지 않는다.
- 최종 수정 시각 — 가장 최근 저장 시점의 시각. 최초 저장 시에는 생성 시각과 같다.
- 관계: DiaryEntry는 DailyLog와 날짜를 공유할 수 있으나 서로를 참조하지 않는 독립된 데이터다. 한 날짜에 0개 이상의 DiaryEntry가 존재할 수 있다.

**DiaryDraft**
- 본문 — 저장되지 않은 편집 중 텍스트. 최대 5,000자.
- 편집 대상 구분 — 새 일기 작성 중인지, 기존 일기 수정 중인지
- 대상 일기의 고유 식별자 — 기존 일기 수정 중인 경우에만 유효
- 관계: DiaryDraft는 앱 전체에 최대 1개만 존재한다. 기존 일기를 수정 중인 경우 해당 DiaryEntry를 고유 식별자로 참조하되, 저장 전까지 그 DiaryEntry의 내용에는 영향을 주지 않는다.

---

## 7. Data Lifecycle & Persistence

### 7.1 Data Ownership
- 모든 데이터는 사용자의 로컬 브라우저 환경에 귀속되며, 외부 서버로 전송되지 않는다.

### 7.2 Storage
- localStorage를 사용하며, 설정 / 일별 로그 / 진행 중 타이머 상태 / 일기 목록 / 작성 중 초안을 각각 별도의 키로 분리하여 저장한다.
- 지원 브라우저(Chrome/Edge/Firefox 최신 데스크톱 버전)에서 `file://` 프로토콜로 직접 실행 시의 localStorage 동작을 최선 노력 기준으로 지속성 지원 대상으로 하며, 쓰기 실패 시의 동작은 EC-05를 따른다.

### 7.3 Synchronization
- 서버가 존재하지 않으므로 로컬-서버 간 동기화는 발생하지 않는다.

### 7.4 Retention
- 별도의 만료 기한 없이, 사용자가 브라우저 저장소 데이터를 직접 삭제하기 전까지 보존된다.
- 작성 중 초안은 저장 조작이 완료되거나 편집 대상이 전환될 때까지만 보존된다.

### 7.5 Deletion
- 사용자가 브라우저의 사이트 데이터/캐시를 삭제할 경우 설정, 로그, 일기, 작성 중 초안, 진행 중 타이머 상태가 모두 함께 소실된다.
- 앱 자체 내에는 별도의 데이터 삭제 기능이 요구사항으로 승인되지 않았다. 이 원칙은 일기에도 동일하게 적용되어, 앱 내에서 일기를 삭제하는 기능은 제공하지 않는다(3.2).

---

## 8. Error & Edge Cases

### EC-01. 브라우저 알림 권한 거부 또는 Notification API 미지원
- 발생 조건: 사용자가 Notification API 권한 요청을 거부했거나, 브라우저가 Notification API 자체를 지원하지 않는 경우
- 시스템 상태: 브라우저 알림 발송 불가
- 사용자에게 표시되는 결과: 소리 알림과 탭 제목 변경으로 세션 종료를 알림
- 데이터 처리: 영향 없음
- 재시도/복구/롤백 정책: 별도 재요청 없이 매 세션 종료 시 폴백 수단으로 대체
- 최종 상태: 앱이 실행 중인 동안, 사용자가 최소 하나의 알림 수단으로 세션 종료를 인지할 수 있는 상태
- 참고: 권한이 허용되고 API 호출이 성공해도 OS/브라우저 설정으로 실제 알림이 표시되지 않는 경우는 감지·보정 대상에서 제외한다(3.2, FR-02). 소리 알림이 이 경우의 실질적 보완 수단 역할을 한다.

### EC-02. 탭 비활성화(백그라운드) 중 세션 종료
- 발생 조건: 브라우저 탭이 비활성 상태인 동안 세션의 종료 목표 시각이 지남
- 시스템 상태: 브라우저의 백그라운드 스로틀링으로 화면 갱신이 지연될 수 있음
- 사용자에게 표시되는 결과: 탭이 다시 활성화되는 즉시(visibilitychange) 종료 목표 시각과 현재 시각을 비교해 즉시 세션 종료 처리 및 알림/메모 입력 흐름 진행. 단, 이 보정은 탭 복귀 시점 기준의 정확한 상태 재계산을 의미하며, 백그라운드 상태에서의 실시간 알림 발생을 보장하는 것은 아니다.
- 데이터 처리: 실제 경과 시간은 절대시각 계산으로 정확히 유지되며, 화면 갱신 지연과 무관하게 데이터 정합성 영향 없음. 이 보정 과정의 화면 갱신은 BR-06에 따라 편집 중인 일기 본문을 덮어쓰지 않는다.
- 재시도/복구/롤백 정책: 해당 없음 (자동 재계산으로 보정)
- 최종 상태: 탭 복귀 시점에 실제 시간 기준으로 정확히 보정된 상태

### EC-03. 새로고침 / 브라우저 재시작 시 진행 중이던 세션 복원 (다중 세션 만료 포함)
- 발생 조건: 타이머가 Running 또는 Paused 상태에서 새로고침 또는 브라우저 재시작 발생. 오프라인/종료 기간 동안 하나 이상의 세션이 만료되었을 수 있음
- 시스템 상태: localStorage에 저장된 진행 중 타이머 상태를 로드
- 사용자에게 표시되는 결과:
  - **Running으로 저장된 경우**: 종료 목표 시각이 아직 남아 있다면 복원된 세션과 남은 시간을 그대로 표시. 이미 지났다면, 경과한 세션 수와 무관하게 '중단 시점의 세션 1회만' 종료 처리(알림/메모 입력 포함)한 뒤 다음 세션을 Idle 상태로 대기시킨다(BR-02).
  - **Paused로 저장된 경우**: `endTimestamp`가 없으므로 만료 판단을 하지 않는다. 저장된 남은 시간 스냅샷을 그대로 Paused 상태로 복원한다(FR-08).
- 데이터 처리: Running 세션은 저장된 endTimestamp와 현재 시각을 비교하여 상태 재계산. 완료 처리된 세션의 로그는 원래 종료 목표 시각 기준 날짜로 귀속된다. Paused 세션은 별도 재계산 없이 스냅샷을 그대로 사용한다.
- 재시도/복구/롤백 정책: 해당 없음 (저장된 절대시각 기준 재계산 및 1회 종료 처리 규칙으로 항상 유효한 상태 도출)
- 최종 상태: Running이었던 세션은 이전과 동일한 진행 상태로 복귀하거나 '1회 종료 처리 + 다음 세션 Idle 대기'로 정리되고, Paused였던 세션은 오프라인 시간과 무관하게 동일한 남은 시간으로 복귀한다. 이 복원 과정에서 세션 복원이 `Memo-Input-Pending`으로 귀결된 경우, FR-13의 표시가 함께 부여된다.

### EC-04. 시스템 시계(로컬 클록) 변경
- 발생 조건: 세션 시작·재개·탭 복귀(visibilitychange) 시점마다 `dateAnchor = Date.now()`와 `perfAnchor = performance.now()`를 함께 샘플링해 갱신한다. 이후 매 확인 시점(예: 화면 갱신 tick, 탭 복귀 등)마다 `dateDelta = Date.now() - dateAnchor`와 `perfDelta = performance.now() - perfAnchor`를 계산하고, `|dateDelta - perfDelta|`가 임계값(5초) 이상이면 시스템 시계 변경으로 판정한다. (두 API는 기준점(origin)이 다르므로 원시 값이 아닌 앵커 대비 델타끼리 비교한다. 앵커를 탭 복귀 시점마다 재설정하므로, 절전·백그라운드 동결 등으로 인한 정상적인 시간 경과는 재설정 직후 새로 측정되어 오탐지를 유발하지 않는다.)
- 시스템 상태: 해당 세션을 즉시 Paused 상태로 전환한다.
- 사용자에게 표시되는 결과: "시스템 시간 변경이 감지되어 타이머가 일시정지되었습니다"라는 확인 알림을 표시하고, 재개 버튼을 제공한다.
- 데이터 처리: 세션은 Paused 상태로 저장되며, 남은 시간은 이상 감지 직전 마지막으로 유효했던 계산값으로 고정된다. 임의로 시간을 연장하거나 단축하지 않는다. 이 처리에 수반되는 화면 갱신은 BR-06에 따라 편집 중인 일기 본문을 덮어쓰지 않는다.
- 재시도/복구/롤백 정책: 사용자가 재개를 선택하면 현재 시각을 기준으로 새 종료 목표 시각을 계산하고 앵커를 재설정하여 Running으로 전환한다. 사용자가 재개하지 않으면 Paused 상태로 유지된다.
- 최종 상태: 사용자 확인 후에만 타이머가 재개되며, 시계 변경으로 인한 임의의 시간 연장·단축 없이 안전하게 처리된 상태

### EC-05. localStorage 쓰기 실패
- 발생 조건: 저장 공간 초과, 브라우저 정책(예: 특정 시크릿 모드 제한) 등으로 localStorage 쓰기가 실패하는 경우. 설정·로그·타이머 상태뿐 아니라 일기 저장과 초안 보관의 실패도 이 경우에 포함된다.
- 시스템 상태: 저장소 상태가 Write-Failed로 전환된다(6.2). 타이머 및 현재 세션 진행은 중단되지 않고 메모리 상에서 계속 동작하며, 일기 편집 영역의 내용도 화면에서 유지된다.
- 사용자에게 표시되는 결과: "데이터가 저장되지 않고 있습니다 — 새로고침 시 최근 변경사항이 유실될 수 있습니다"라는 경고 배너를 화면에 표시한다.
- 데이터 처리: 실패한 쓰기는 이후 상태 변경 시점마다 자동으로 재시도한다. 재시도가 성공하면 저장소 상태를 Synced로 되돌리고 경고를 해제한다. 일기 저장 조작이 실패한 경우에도 메모리 상의 일기 목록에는 반영되어 화면에 표시되며, 이후 재시도로 저장이 성공하면 정상 보존된다.
- 재시도/복구/롤백 정책: 자동 재시도만 수행하며, 별도의 수동 재시도 UI는 두지 않는다. 사용자 조작(타이머 시작, 일기 작성 등)을 막지 않는다.
- 최종 상태: 저장이 재개되면 정상 상태로 복귀하고, 실패가 지속되는 동안은 경고가 표시된 채로 앱 사용은 계속 가능하다.

### EC-06. 일기 본문 글자 수 상한 초과
- 발생 조건: 사용자가 일기 편집 영역에 5,000자를 초과하는 분량을 입력하려 하는 경우(직접 타이핑 및 붙여넣기 포함)
- 시스템 상태: 편집 영역의 본문은 5,000자를 넘지 않는 상태로 유지된다. 앱의 다른 기능은 영향을 받지 않는다.
- 사용자에게 표시되는 결과: 편집 영역에 현재 글자 수와 상한이 함께 표시되어 있으며, 상한에 도달하면 더 이상 입력이 반영되지 않고 상한에 도달했음을 인지할 수 있다.
- 데이터 처리: 상한을 초과하는 부분은 저장되지 않는다. 이미 입력된 5,000자 이내의 내용은 그대로 보존되며 유실되지 않는다. 초안 보관에도 동일한 상한이 적용된다.
- 재시도/복구/롤백 정책: 해당 없음. 사용자가 직접 내용을 줄여 상한 이내로 조정한다.
- 최종 상태: 저장되는 일기 본문과 보관되는 초안 본문이 항상 5,000자 이하인 상태

---

## 10. Non-Functional Requirements

### NFR-01. 타이머 정확도 (Reliability)
- 절대시각(Date.now()) 기반 계산 방식을 사용하여, OS 시스템 시계가 외부 요인으로 변경되지 않고 정상적으로 유지되는 동안에는 수 시간 이상 연속 구동해도 표시 시간과 실제 경과 시간 간 누적 오차(드리프트)가 발생하지 않아야 한다.
- 시스템 시계 변경 여부는 EC-04에 정의된 델타 비교 알고리즘(`Date.now()`와 `performance.now()`의 앵커 대비 델타 차이, 임계값 5초)으로 감지하며, 감지된 경우의 동작은 EC-04를 따른다.

### NFR-02. 오프라인 가용성 (Availability / Compatibility)
- 인터넷 연결이 전혀 없는 환경에서도 모든 기능이 정상 동작해야 한다.
- 외부 CDN, 웹폰트, 외부 API 등 네트워크 리소스에 의존하지 않아야 한다.

### NFR-03. 이식성 (Portability)
- 단일 HTML 파일만으로 배포·실행이 가능해야 하며, 별도의 설치나 서버 구동 없이 브라우저에서 파일을 여는 것만으로 동작해야 한다.
- §1.3에 명시된 지원 브라우저(Chrome/Edge/Firefox 최신 데스크톱 버전) 범위 내에서 이식성을 보장한다.

### NFR-04. 작성 중 데이터 보존성 (Reliability)
- 사용자가 일기 편집 영역에 입력한 내용은, 저장 조작 이전이라도 앱 내부의 화면 갱신(타이머 주기 갱신, 세션 종료 처리, 시계 변경 감지, 설정 저장, 경고 표시 등)에 의해 임의로 지워지거나 이전 값으로 되돌아가지 않아야 한다(BR-06).
- 저장 조작 이전에 새로고침 또는 브라우저 재시작이 발생하더라도, 마지막으로 보관된 초안 시점까지의 내용이 복원되어야 한다(FR-12).

---

## 12. Technical Constraints & Approved Technical Decisions

### 12.1 Technical Constraints
- 단일 HTML 파일로 구현하며 서버를 사용하지 않는다.
- 완전 오프라인 동작을 위해 외부 네트워크 리소스(CDN, 웹폰트, 외부 API, 외부 음원 파일 등)를 일체 사용하지 않는다.
- 별도의 프레임워크(React 등)를 사용하지 않고 순수 HTML/CSS/JavaScript로 구현한다.
- 아이콘 등 그래픽 요소는 인라인 SVG로 구현한다.
- 지원 브라우저는 Chrome/Edge/Firefox 최신 데스크톱 버전(`file://` 실행 기준)으로 한정한다.

### 12.2 Approved Technical Decisions
- 타이머 계산 방식: `setInterval` 카운트다운이 아닌, `Date.now()` 기반 절대 종료 시각(endTimestamp) 계산 방식을 사용한다.
- 백그라운드 보정: Page Visibility API(`visibilitychange`)를 사용해 탭 비활성 후 복귀 시 즉시 상태를 재계산한다.
- 알림음: Web Audio API(OscillatorNode 등)로 소리를 직접 합성하며, 외부 음원 파일을 사용하지 않는다. 최초 사용자 상호작용 시점에 오디오 컨텍스트를 활성화한다.
- 브라우저 알림: Notification API를 사용하고, 권한이 허용된 경우 소리와 항상 동시 발송하며, 권한이 거부되었거나 API가 지원되지 않는 경우 소리 알림과 탭 제목 변경으로 대체한다. OS/브라우저 설정에 의한 알림 미표시는 감지 대상에서 제외한다.
- 데이터 저장: `localStorage`를 사용하며, 설정 / 일별 로그 / 진행 중 타이머 상태 / 일기 목록 / 작성 중 초안을 각각 별도 키로 분리 저장한다. 쓰기 실패 시 EC-05에 따라 경고 후 자동 재시도한다.
- 세션 흐름 제어: 상태 머신 방식으로 Focus / Short Break / Long Break 사이클을 관리하며, 세션 상태는 Idle/Running/Paused로 명시적으로 구분한다.
- 사이클 카운트: Focus 슬롯은 완료·스킵과 무관하게 소모되는 방식(슬롯 소모형)으로 4회차 Long Break 전환을 판단한다.
- 리셋 정책: 리셋은 현재 세션 타이머만 초기화하며, 사이클 슬롯 소모 카운트는 유지한다(BR-03).
- 자동 시작 범위: 세션 자동 시작은 실시간 사용 중(포그라운드 실행 중)에만 적용하며, 재접속으로 복원된 만료 세션은 자동 시작하지 않고 Idle로 대기시킨다(BR-02).
- **시스템 시계 변경 대응 알고리즘(승인된 결정, Implementation Freedom 제외 대상)**: 세션 시작·재개·탭 복귀 시점마다 `Date.now()`와 `performance.now()`를 앵커로 샘플링하고, 이후 각 클록의 앵커 대비 델타(`dateDelta`, `perfDelta`)를 비교하여 그 차이가 5초 이상이면 시계 변경으로 판정, 세션을 일시정지하고 사용자 확인 후 재개한다(EC-04). 원시 값의 직접 비교는 사용하지 않는다.
- 설정 적용 시점: Idle 세션은 설정 변경 즉시 반영, Running/Paused 세션은 해당 세션 종료 후 다음 세션부터 반영한다(FR-07).
- Memo-Input-Pending 상태에서는 타이머 제어 버튼(시작/일시정지/리셋/스킵)을 노출하지 않는다(BR-04).
- Paused 세션의 재접속 복원: endTimestamp 기반 만료 판단을 적용하지 않고 남은 시간 스냅샷을 그대로 복원한다(FR-08, EC-03).
- 오프라인 중 다중 세션 만료: 경과 세션 수와 무관하게 1회만 종료 처리한다.
- **일기 데이터 구조**: 날짜를 키로 하는 사전(dictionary) 구조가 아니라, 각 항목이 고유 식별자를 갖는 목록(list) 구조로 저장한다(BR-05). 같은 날짜에 여러 편의 일기 저장을 허용하기 위한 결정이다.
- **일기 고유 식별자 생성 방식(승인된 결정, Implementation Freedom 제외 대상)**: 생성 시점의 시각 값과 순번(카운터)의 조합으로 생성하며, 난수 생성 API(`crypto.randomUUID` 등)에 의존하지 않는다. `file://` 환경과 지원 브라우저 전반에서의 동작 보장을 위한 결정이다.
- **일기 목록 정렬 방향(승인된 결정)**: 날짜 기준 내림차순(최신 우선), 동일 날짜 내에서는 생성 시각 기준 내림차순으로 정렬한다(BR-05). 작업 로그 화면의 메모 정렬(오름차순, FR-06)과는 독립적이다.
- **일기 구성**: 제목 필드 없이 본문만으로 구성한다. 목록의 항목 표시는 날짜와 본문 앞부분 미리보기를 사용한다.
- **일기 날짜 부여**: 최초 저장 시점의 로컬 날짜로 자동 부여하며, 사용자 지정 및 이후 변경을 허용하지 않는다.
- **일기 본문 길이 상한**: 5,000자. 저장되는 본문과 보관되는 초안 모두에 동일하게 적용한다(EC-06).
- **일기 저장 방식**: 명시적 저장 조작으로만 일기 목록에 확정 반영하며, 저장 이전의 편집 내용은 별도의 초안 데이터로 자동 보관한다(FR-10, FR-12). 초안 보관은 입력이 멈춘 뒤 일정 시간 경과 시점에 1회 수행하며, 매 입력마다 저장하지 않는다.
- **초안 개수**: 앱 전체에 최대 1개만 보관하며, 가장 최근에 편집 중이던 대상만을 가리킨다(FR-12).
- **편집 중 입력 보호(승인된 결정, Implementation Freedom 제외 대상)**: 일기 편집 영역의 내용은 사용자 입력, 편집 대상 전환, 초안 복원 이외의 어떤 화면 갱신으로도 변경되지 않아야 한다(BR-06). 이 보호를 구현하는 구체적 방식은 자유이나, 보호 자체를 생략하거나 완화할 수 없다.
- **일기 삭제 기능 미제공**: 기존 §7.5의 삭제 기능 미승인 원칙을 일기에도 동일하게 적용한다.
- **화면 밖 세션 종료 표시**: `Memo-Input-Pending` 상태 동안 상단 주요 메뉴의 타이머 항목에 시각적 표시를 부여한다(FR-13).
- **일기 화면 배치**: 목록 영역과 편집 영역을 함께 보여주는 2단 구성으로 제공한다(FR-09).

### 12.3 Implementation Freedom
- 위에서 명시되지 않은 내부 구현 세부사항(클래스/함수/변수 명명, 내부 파일 구조, 완료 카운트·슬롯 소모의 저장 순서 등 세부 알고리즘, CSS 구조, 오디오 컨텍스트 활성화의 구체적 구현, Notification 권한 요청 시점, 초안 보관 대기 시간의 구체적 값, 목록 미리보기 글자 수와 말줄임 방식, 좁은 화면에서의 2단 배치 전환 기준, 세션 종료 표시의 시각적 형태, 편집 중 입력 보호의 구현 기법 등)은 본 PRD의 승인된 내용을 위배하지 않는 한 Development AI가 합리적으로 결정할 수 있다.
- 세부 디자인과 UI(색상, 레이아웃, 애니메이션 등)는 사용자가 AI 재량으로 위임하였으므로, 위 기술 제약과 승인된 기술적 결정을 해치지 않는 범위 내에서 Development AI가 자유롭게 결정한다.
- **제외 사항**: 다음 세 가지는 Implementation Freedom의 대상이 아니며, 다른 방식으로 대체할 수 없다.
  1. 시스템 시계 변경 감지 알고리즘 (EC-04, 12.2에 델타 비교 방식으로 명시)
  2. 일기 고유 식별자 생성 방식 (BR-05, 12.2에 시각+순번 조합으로 명시, 난수 API 의존 금지)
  3. 편집 중 입력 보호의 적용 여부 (BR-06, 12.2 — 구현 기법은 자유이나 보호 자체는 생략 불가)

---

## 13. Acceptance Criteria

### 13.1 Functional Acceptance
- FR-01 ~ FR-13에 정의된 모든 기능이 각 기능별 Acceptance Criteria를 충족한다.
- Paused 상태에서 새로고침/재접속해도 남은 시간 스냅샷이 오프라인 시간과 무관하게 그대로 복원된다(FR-08, EC-03).
- `Memo-Input-Pending` 상태에서는 타이머 제어 버튼이 노출되지 않고, 메모 제출/건너뛰기 UI만 노출된다(BR-04).
- Idle 세션의 설정 변경은 화면에 즉시 반영되고, Running/Paused 세션의 설정 변경은 다음 세션부터 반영된다(FR-07).
- localStorage 쓰기가 실패해도 타이머 사용과 일기 작성이 중단되지 않고, 저장 실패를 알리는 경고가 표시된다(EC-05).
- 일기 목록이 날짜 기준 최신순으로 정렬되며, 같은 날짜에 여러 편의 일기가 각각 별개 항목으로 보존된다(FR-09, FR-10, BR-05).
- 기존 일기를 수정해도 그 일기의 날짜와 항목 개수가 변하지 않는다(FR-11).
- 저장하지 않은 편집 내용이 새로고침 이후에도 초안으로 복원되며, 미저장 상태임이 표시된다(FR-12).
- 일기 본문이 5,000자를 넘지 않으며, 상한 도달 시에도 기존 입력 내용이 유실되지 않는다(EC-06).
- `Memo-Input-Pending` 상태 동안 상단 타이머 메뉴에 표시가 나타나고, 메모를 제출하거나 건너뛰면 사라진다(FR-13).

### 13.2 System Acceptance
- **정상 실행 중 시나리오**: 앱이 포그라운드에서 실행 중인 동안에는 타이머, 사이클 전환(자동 시작 포함), 스킵, 메모, 로그, 설정이 서로 연동되어 하나의 뽀모도로 세션 흐름(시작→종료→기록/자동전환→다음 세션)이 끊김 없이 동작한다.
- **재접속 복원 시나리오**: 앱이 종료되어 있던 동안 세션이 만료된 뒤 재접속한 경우, 정상 흐름과 달리 '1회 종료 처리 + 다음 세션 Idle 대기(수동 시작)'로 동작하며, 이는 BR-02에 따른 의도된 예외로 취급한다. Running/Paused 두 경우 모두 별도 테스트 케이스로 검증한다.
- **일기 작성 전체 흐름 시나리오**: '새 일기' 진입 → 본문 작성 → 저장 → 목록 반영 → 목록에서 재선택 → 수정 → 재저장 → 목록 미리보기 갱신까지가 끊김 없이 연결되어 동작한다.
- **타이머-일기 동시 사용 간섭 없음 시나리오**: 타이머가 Running인 상태에서 일기 화면에 머물며 본문을 작성하는 동안, 타이머의 주기적 갱신과 Focus 세션 종료 처리가 발생해도 작성 중인 본문이 유지되고(BR-06), 세션 종료 사실은 상단 메뉴 표시로 전달되며(FR-13), 이후 타이머 화면으로 이동하면 메모 입력 대기 상태가 정상적으로 남아 있다.
- 인터넷 연결 없이, 지원 브라우저에서 단일 HTML 파일을 여는 것만으로 위 흐름 전체가 정상 동작한다.

### 13.3 User Acceptance
- 사용자가 하루 동안 실제로 앱을 사용했을 때, 로그 화면에서 그날 완료한 뽀모도로 개수와 각 메모(빈 메모 포함)가 실제 작업 내역과 일치함을 확인할 수 있다.
- 사용자가 임의 시점에 새로고침하거나 브라우저를 재시작해도(앱이 다시 실행되는 시점을 기준으로) 진행 중이던 세션, 설정값, 로그, 일기가 그대로 유지됨을 확인할 수 있다.
- 브라우저가 실행 중이고 시스템 시계가 정상 유지되는 동안, 수 시간 이상 타이머를 켜둔 뒤에도 표시된 남은 시간이 실제 경과 시간과 일치함을 확인할 수 있다.
- 시스템 시계가 임의로 변경되면 타이머가 임의로 늘어나거나 줄어드는 대신 일시정지되고 확인을 요청받는다.
- 저장 공간 문제 등으로 데이터가 저장되지 않는 상황에서도, 사용자는 경고를 통해 이를 인지할 수 있다.
- 사용자가 여러 날에 걸쳐 여러 편의 일기를 작성했을 때, 일기 화면의 목록에서 최신 일기부터 순서대로 훑어보고 원하는 항목을 열어 내용을 확인할 수 있다.
- 사용자가 일기를 쓰던 도중 저장하지 않고 앱을 닫았다가 다시 열었을 때, 쓰던 내용이 그대로 남아 있고 아직 저장되지 않았음을 알 수 있다.
- 사용자가 타이머를 켜둔 채 일기를 작성하는 동안, 작성 중이던 글이 사라지지 않으며 집중 세션이 끝났다는 사실도 놓치지 않고 확인할 수 있다.
